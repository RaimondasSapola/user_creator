{% extends 'base.html.twig' %}

{% block body %}
    {% if error %}
        <div id="login-error"><span class="glyphicon glyphicon-exclamation-sign"></span> {{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
    {{ form(login_form) }}
{% endblock %}